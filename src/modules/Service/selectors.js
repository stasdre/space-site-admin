export const types = (state) => state.workTypes.types;
export const isLoading = (state) => state.workTypes.isLoading;
export const isSaved = (state) => state.service.isSaved;
export const services = (state) => state.service.services;
